import React,{Component} from 'react';

// 需要验证 如果登录才能进来 否则跳到登录页

export default class Profile extends Component {
    render(){
        return (
            <div>
                Profile
            </div>
        )
    }
}